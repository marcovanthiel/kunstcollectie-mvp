{
  "name": "kunstcollectie-backend",
  "version": "1.0.0",
  "scripts": {
    "build": "npm install && next build",
    "start": "next start -p $PORT"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
